# North Star & Goals

**As an agent or contributor about to change this codebase, I need one orienting picture of what
we're building and why, so every issue, plan, PR, and review can be checked against it.**

This states the outcomes the project is building toward — the destination, not a status report.
What is shipped versus still in flight is tracked as GitHub issues, not here.

`<FILL: this section is empty until the owner fills it in — see PROJECT-SETUP.md § "Fill in the
blanks". Every issue, plan, PR, and review in this pipeline is checked against whatever goes here,
so do not skip it.>`

## The shift being designed for

> `<FILL: one or two sentences describing the behavior change this project exists to produce — who
changes what they do, and why the project makes that happen.>`

- **End user (whose behavior changes):** `<FILL: name them>`
- **Who it pays off for:** `<FILL: name them, if different from the end user>`

## The goals

`<FILL: list the project's goals, each with a one-line outcome statement. Three to five goals is
typical — enough to check a change against, not so many that everything maps to one of them.>`

## Scope

`<FILL: what this build is for (audience size, timeframe, platform) and what is deliberately out of
scope for it.>`
